"""ai_sentinel — honest retrieval memory, NOT learning.

The agent journal records decisions and matches outcomes against actual
trade P&L. The sentinel runs periodic judgment cycles using the journal
as context. Neither the journal nor the sentinel auto-trades.
"""
